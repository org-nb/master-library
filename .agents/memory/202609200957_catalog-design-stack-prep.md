# Catalog design and retirement boundary

Prepared the `catalog/design` layer as the design-and-retirement boundary for the architecture record set. This branch includes the approved design documents, ADRs, plan/history documents, rendered diagrams, and the legacy ingestion code removal as a deliberate documentation boundary.

This preparation keeps the premature ADR-0001 worker implementation note and the local catalog scaffold out of the commit. Those belong to the later `catalog/implementation` layer after the developer commits this design-and-retirement boundary.
